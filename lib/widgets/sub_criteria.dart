import 'package:flutter/material.dart';

class SubCriteria extends StatelessWidget {
  const SubCriteria({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text('Subcriteria'),
    );
  }
}
